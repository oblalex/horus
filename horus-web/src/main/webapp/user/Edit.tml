<html t:pageTitle="${pageTitle}"
      t:type="layout"
      xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd">
    
    <div class="content_hd">${pageTitle}</div>
    
    <t:user.accountcomponent
        t:id="accountCmpnt"
        isOwnerEditing="${visitorIsOwner}"
        submitTitle="${submitTitle}" />
    
</html>
